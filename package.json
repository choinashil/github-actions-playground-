{
  "name": "ci-cd-poc",
  "version": "1.0.0",
  "private": true,
  "workspaces": [
    "packages/*",
    "apps/*"
  ],
  "scripts": {
    "shared": "yarn workspace @packages/shared",
    "next": "yarn workspace @apps/next-app",
    "react": "yarn workspace @apps/react-app"
  }
}
