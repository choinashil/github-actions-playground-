{
  "name": "ci-cd-poc",
  "version": "1.0.0",
  "private": true,
  "workspaces": [
    "packages/*",
    "apps/*"
  ],
  "scripts": {
    "shared": "yarn workspace @packages/shared",
    "next-app": "yarn workspace @apps/next-app",
    "react-app": "yarn workspace @apps/react-app"
  }
}
